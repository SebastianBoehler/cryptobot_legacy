export type RowDataPacket = {
    id: number
    price: string
    volume: string
    time: string
    bid: string
    ask: string
    open: string
    close: string
    high: string
    low: string
}